on:
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: Tanooki907/radiation-realms-POC/.github/workflows/reusable-workflow.yml@main
    with:
      config-path: .github/labeler.yml
    secrets:
      personal_access_token: ${{ secrets.token }}
